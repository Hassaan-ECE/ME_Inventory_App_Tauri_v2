import packageJson from "../package.json";

export const APP_BASE_NAME = "ME Inventory";
export const APP_VERSION = packageJson.version;
export const APP_DISPLAY_NAME = `${APP_BASE_NAME} v${APP_VERSION}`;
